account_id: ""
actions:
  eek637/governance-alerts:
    runtime: v2
    sources: actions
    specs:
      example:
        description: This is just an example, but you can publish this action.
        function: example:blockHelloWorldFn
        trigger:
          type: block
project_slug: ""
